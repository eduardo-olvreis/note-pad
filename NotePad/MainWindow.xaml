<Window x:Class="NotePad.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NotePad"
        mc:Ignorable="d"
        Closing="Window_Closing"
        Title="MainWindow" Height="291" Width="357" Topmost="True">
    <Grid>
        <Label x:Name="lblPrincipal" Content="Anotações" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top" FontSize="20" FontFamily="Segoe UI Semibold"/>
        <Grid Margin="10,88,10,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0*"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="txtPlaceholder" 
                   Text="Digite suas anotações aqui..." 
                   Foreground="Gray" 
                   Padding="5"
                   IsHitTestVisible="False" Grid.ColumnSpan="2" Margin="3,-31,0,31"/>
            <TextBox x:Name="txtBox" 
                 TextWrapping="Wrap" 
                 Background="Transparent"
                 Padding="5"
                 TextChanged="txtBox_TextChanged" Grid.ColumnSpan="2" Margin="0,-31,0,0"/>
        </Grid>
        <TextBox x:Name="txtNomeArquivo" HorizontalAlignment="Left" Margin="147,29,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="144"/>
        <Label Content="Insira o nome do arquivo:" HorizontalAlignment="Left" Margin="147,2,0,0" VerticalAlignment="Top" RenderTransformOrigin="-0.009,-0.037" Width="144"/>
        <Button x:Name="btnSalvarNomeArquivo" Content="Salvar" HorizontalAlignment="Left" Margin="296,29,0,0" VerticalAlignment="Top" Height="18" Click="btnSalvarNomeArquivo_Click"/>
    </Grid>
</Window>
