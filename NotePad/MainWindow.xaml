<Window x:Class="NotePad.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NotePad"
        mc:Ignorable="d"
        Title="MainWindow" Height="291" Width="357" Topmost="True">
    <Grid>
        <Label x:Name="lblPrincipal" Content="Anotações" HorizontalAlignment="Center" Margin="0,10,0,0" VerticalAlignment="Top" FontSize="20" FontFamily="Segoe UI Semibold"/>
        <Grid Margin="10,88,10,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0*"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <TextBlock x:Name="txtPlaceholder" 
                   Text="Digite suas anotações aqui..." 
                   Foreground="Gray" 
                   Padding="5"
                   IsHitTestVisible="False" Grid.ColumnSpan="2" Margin="3,-31,0,31"/>
            <TextBox x:Name="txtBox" 
                 TextWrapping="Wrap" 
                 Background="Transparent"
                 Padding="5"
                 TextChanged="txtBox_TextChanged" Grid.ColumnSpan="2" Margin="0,-31,0,0"/>
        </Grid>
    </Grid>
</Window>
